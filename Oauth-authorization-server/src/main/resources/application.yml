server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_registration?createDatabaseIfNotExist=true
    username: root
    password: mynewpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
